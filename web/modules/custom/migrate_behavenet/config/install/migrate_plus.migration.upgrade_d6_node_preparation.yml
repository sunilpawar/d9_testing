langcode: en
status: true
dependencies:
  enforced:
    module:
      - migrate_behavenet
id: upgrade_d6_node_preparation
migration_tags:
  - 'Drupal 6'
migration_group: migrate_drupal_6
label: 'Nodes (preparation)'
source:
  plugin: behavenet_nodes_basic
  node_type: preparation
process:
  nid: nid
  # Allow the vid to auto-generate otherwise we get conflicts from stubbed nodes
  # that were generated by earlier imports.
  # vid: vid
  type: type
  langcode:
    plugin: default_value
    source: language
    default_value: und
  title: title
  uid: node_uid
  status: status
  created: created
  changed: changed
  promote: promote
  sticky: sticky
  revision_uid: revision_uid
  revision_log: log
  revision_timestamp: timestamp

  field_credit: field_tt_extras_credit
  field_prep_dea_number: field_generic_dea
  field_prep_description/format:
    plugin: default_value
    default_value: full_html
  field_prep_description/value: field_general_description/value
  field_graphic: field_generic_graphic
  field_outside_media: field_outside_video
  field_related_terms: all_related_terms
  field_schedule: field_generic_schedule
  field_slang: field_gen_slang_terms
  field_source: field_generic_sources
destination:
  plugin: 'entity:node'
migration_dependencies:
  required: { }
  optional: { }
