langcode: en
status: true
dependencies:
  enforced:
    module:
      - migrate_behavenet
id: upgrade_d6_node_compound
migration_tags:
  - 'Drupal 6'
migration_group: migrate_drupal_6
label: 'Nodes (compound)'
source:
  plugin: behavenet_nodes_basic
  node_type: compound
process:
  nid: nid
  # Allow the vid to auto-generate otherwise we get conflicts from stubbed nodes
  # that were generated by earlier imports.
  # vid: vid
  type: type
  langcode:
    plugin: default_value
    source: language
    default_value: und
  title: title
  uid: node_uid
  status: status
  created: created
  changed: changed
  promote: promote
  sticky: sticky
  revision_uid: revision_uid
  revision_log: log
  revision_timestamp: timestamp

  field_slang: field_gen_slang_terms
  field_generic: field_compound_drug
  field_related_terms: all_related_terms
  field_related_content: incoming_related_content
  field_description/format:
    plugin: default_value
    default_value: full_html
  field_description/value: field_general_description/0/value
  field_graphic: field_generic_graphic
  field_outside_media: field_outside_video
  field_credit: field_tt_extras_credit

destination:
  plugin: 'entity:node'
migration_dependencies:
  required: { }
  optional: { }
